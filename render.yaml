services:
  - type: web
    name: og-ai-service
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python ai_agent.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
    healthCheckPath: /health
